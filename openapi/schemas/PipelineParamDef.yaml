# schemas/orm/PipelineParamDef.yaml
type: object

properties:
  ID:
    type: integer
    nullable: false
    readOnly: true
    description: none
    example: 0
  PipelineDefID:
    type: integer
    description: none  
  Name:
    type: string
    # any char with length 0-30 only
    pattern: ^.{0,30}
    description: none
  DataType:
    type: string
    # any char with length 0-6 only
    pattern: ^.{0,6}
    description: none